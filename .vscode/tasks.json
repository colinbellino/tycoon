{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build & run (mac)",
            "type": "shell",
            "command": "make build run"
        },
        {
            "label": "watch & run (mac)",
            "type": "shell",
            "command": "make dev run",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}